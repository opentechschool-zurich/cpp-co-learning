CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(IPAddress)

ADD_DEFINITIONS("-std=c++11")

FIND_PACKAGE( Boost REQUIRED COMPONENTS regex)

INCLUDE_DIRECTORIES( ${Boost_INCLUDE_DIRS} )

ADD_EXECUTABLE (IPAddress
    main.cpp
)

TARGET_LINK_LIBRARIES(IPAddress
    ${Boost_LIBRARIES}
)
