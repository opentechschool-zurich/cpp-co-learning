CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT("histogramm")

ADD_DEFINITIONS("-std=c++11")

FIND_LIBRARY(UI_LIBRARY ui HINTS ${CMAKE_CURRENT_SOURCE_DIR}/libui)

ADD_EXECUTABLE (histogramm
    main.cpp
)

TARGET_LINK_LIBRARIES(histogramm PUBLIC ${UI_LIBRARY})
